# ONBOARD Case: Clear Reference

Use `mood-diary-studio` in ONBOARD mode.

Proposed name: Paper Dot

Reference: `skill/mood-diary-studio/assets/sample-character/references/front.png`

The reference is authorized for this task. Produce a candidate mascot `character.md` from the image alone. Do not read the existing sample card, do not install anything, and do not invent personality or worldbuilding.

The user has identified `/example/private-character-pack` as a future local-filesystem pack root, but has not approved this candidate or any write. Report the binding accurately and keep the result not installed.
