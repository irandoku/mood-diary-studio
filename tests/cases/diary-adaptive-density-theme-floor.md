# DIARY Case: Adaptive density with a theme floor

Mode: DIARY

Use the approved bundled Paper Dot sample character and its authorized public
reference. The character is a compact mascot with limited pose and expression
variation. Do not modify the character card or generate an image.

Daily account:

> After the first rain of summer, I planted a basil cutting beside the kitchen
> window. The small beginning made me feel quietly hopeful.

Create one model-neutral mood diary brief. The user requests a low-density page
with visible breathing space; the expected fit is `sparse`, not `minimal`,
because the topic still needs non-text carriers. It does not need the layered
profile.

Expected:

- choose density from the scene's narrative need rather than forcing a fixed
  profile, and report the requested preference (`low`), selected profile
  (`sparse`), and rationale;
- preserve non-text carriers such as the basil cutting, planting relationship,
  damp window-side light, or visible soil, using only details supported by the
  entry or clearly marked editorial staging within the mood-to-scene boundary;
- classify the carriers in the budget, for example one supporting object and
  two environmental cues, without hiding role-sharing or inflating the count;
- pass caption removal: without the phrase, the scene still reads as a small
  post-rain planting / new beginning rather than generic reading or posing;
- pass character substitution: the scene remains identifiable if the mascot is
  replaced by a neutral figure;
- keep the hand-drawn diary breathing space while avoiding a blank generic
  background and avoiding an invented prop list.
- Because the entry does not request a more specific rendering treatment, use
  `bright-fine-line-gongbi-diary` as the framework-default effective entry
  style without changing the approved character card.
