# Case: clear reference uses quick review

Mode: ONBOARD

Reference: `skill/mood-diary-studio/assets/sample-character/references/front.png`

The image is authorized for this test. Draft a new mascot candidate from the
reference. Do not read the existing bundled sample card. Use the easiest safe
review experience. Do not install or save anything.

Expected:

- select `quick` review because the input has no stated conflicts;
- show one approval summary with must-remain, may-vary, left-undecided, style,
  and storage outcome before detailed evidence;
- ask the single approve-or-revise question immediately after the summary;
- retain the complete evidence ledger and candidate card in the same turn,
  preferring a separate response artifact, then an accessible collapsed
  section, then an inline appendix;
- identify the delivery method and artifact label;
- keep the complete artifact concise instead of repeating every observation in
  both surrounding prose and the candidate;
- stay within the quick-review content budget;
- use `mood-diary-character/v2`;
- use `diary-default`, `strength: soft`, and
  `per_entry_override: allowed`;
- ask no storage question and perform no write;
- report `availability_state: in-context` and
  `persistence_state: export-ready`.
