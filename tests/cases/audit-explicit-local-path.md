# AUDIT Case: Explicit Local Path

Use `mood-diary-studio` in AUDIT mode with the approved Paper Dot character card
at the exact accessible absolute path
`/example/private-character-pack/paper-dot/character.md`.

The path happens to be inside the host's current workspace. The host can read
and write it, but the user has not approved any update.

A new authorized reference shows the same paper-dot silhouette wearing a yellow
raincoat. Treat the raincoat as a contextual variant. Report the storage
binding, propose any appropriate patch, and do not modify the card.
