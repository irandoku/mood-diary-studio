# Case: v1 card has no inferred style canon

Mode: DIARY

Use the approved public v1 sample card and its authorized reference. The diary
entry asks for a quiet scene in a simple cut-paper medium.

Expected:

- read the v1 card without migration;
- preserve its approved identity anchors;
- report stored style policy `per-entry` and stored override rule `allowed` as
  v1 compatibility values;
- report the requested cut-paper treatment separately as the effective entry
  style sourced from the current request;
- do not revise the card or claim the reference caused the output style.
