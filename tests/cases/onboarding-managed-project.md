# ONBOARD Case: Managed Project

Use `mood-diary-studio` in ONBOARD mode.

Proposed name: Paper Dot

Reference: `paper-dot-front.png`, attached to the current managed web project.

The host exposes project sources for reading but does not expose a local directory or direct Skill write-back. Produce the complete candidate card and explain how it should be returned for the user to add to project sources. Do not invent an absolute path, do not claim filesystem installation, and do not place the character under bundled Skill assets.

Report `availability_state: in-context` and
`persistence_state: export-ready`. Do not claim that the Skill can invoke the
host's add-source UI.
